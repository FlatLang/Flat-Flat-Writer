package flat/writer/flat

import flat/ast
import flat/compiler/models/Token
import flat/io
import flat/log/Logger
import flat/eventstream/EventStream
import flat/writer

class extends NodeWriter<LocalDeclarationNode> {
  public write(LocalDeclarationNode node, OutputStream outputStream) {
    if (node.type) {
      outputStream.write(node.type)
    }

    for (i in 0..node.arrayDimensions) {
      outputStream.write("[]")
    }

    outputStream.write(node.name)
  }
}
